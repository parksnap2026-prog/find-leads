# Optional: add your Google Places API key for richer data (phone numbers, ratings).
# Without it the app uses OpenStreetMap (free, no sign-up needed).
GOOGLE_PLACES_API_KEY=

# Port (default 5050)
PORT=5050
